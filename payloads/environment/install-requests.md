# Install requests

Agents: append one line per thing that must be baked into the image (apt package, system library, PATH entry).
Format: `- YYYY-MM-DD | agent id | package/binary | why | how verified`

